# Em desenvolvimento local, deixe vazio/nao defina — o proxy do Vite (vite.config.ts) ja
# encaminha /api e /ws para http://localhost:4000.
#
# Em producao (Vercel), defina com a URL publica do backend no Render, SEM barra no final:
# VITE_API_BASE_URL=https://candles-12-server.onrender.com
VITE_API_BASE_URL=
