{
  "name": "polarium-12candles-app",
  "version": "0.1.0",
  "private": true,
  "description": "Monitor e (futuramente) automacao da estrategia '12 Candles' (M1) na Polarium/Quadcode. NAO opera com dinheiro real ate o modo REAL ser explicitamente destravado.",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "postinstall": "npm run build --workspace shared",
    "dev": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace shared && npm run build --workspace server && npm run build --workspace client",
    "test": "npm run test --workspace server",
    "typecheck": "npm run typecheck --workspace shared && npm run typecheck --workspace server"
  }
}
